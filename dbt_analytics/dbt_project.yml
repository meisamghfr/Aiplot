name: aiplot_analytics
version: '1.0.0'
config-version: 2
profile: aiplot_analytics
model-paths: ['models']
test-paths: ['tests']
clean-targets: ['target', 'dbt_packages']

models:
  aiplot_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +schema: analytics
